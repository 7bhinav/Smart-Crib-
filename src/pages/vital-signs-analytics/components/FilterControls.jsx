import React from 'react';

// FilterControls intentionally renders nothing to remove the three select boxes
// per user request. Keep the component exported so parent pages don't error.
const FilterControls = () => null;

export default FilterControls;